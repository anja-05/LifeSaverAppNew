<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Basisthema -->
    <style name="Base.Theme.LifeSaver" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Hintergrundfarbe für alle Screens -->
        <item name="android:windowBackground">@color/warm_white</item>

        <!-- Statusbar-Farbe (oben) -->
        <item name="android:statusBarColor">@color/warm_white</item>

        <!-- BottomNavigationView: Auswahlhintergrund -->
        <item name="colorSecondaryContainer">@color/medium_red</item>
    </style>

    <!-- App-Theme -->
    <style name="Theme.LifeSaver" parent="Base.Theme.LifeSaver" />
</resources>

